<UserControl x:Class="Elden_Ring_Debug_Tool_WPF.Views.FaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wpfTool="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Elden_Ring_Debug_Tool_WPF" 
             xmlns:vm="clr-namespace:Elden_Ring_Debug_Tool_ViewModels.ViewModels;assembly=Elden-Ring-Debug-Tool-ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800" d:Background="{StaticResource Background}" DataContextChanged="OnDataContextChanged"
             d:DataContext="{d:DesignInstance vm:FaceViewViewModel, IsDesignTimeCreatable=True}" 
                    IsVisibleChanged="OnIsVisibleChanged">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Label Content="Bytes:"></Label>
            <TextBlock Text="{Binding HexString}" 
                       TextWrapping="Wrap" 
                       FontSize="14"
                       FontFamily="Consolas"
                       Foreground="Aqua"/>
            <Button Content="Save Face Data" Background="Red" Command="{Binding SaveFaceDataCommand}"/>
            
        </StackPanel>

    </Grid>


</UserControl>
